comment delim "#\|" "\|#" multiline nested
comment start ";"

vardef SPECIALCHAR = '\\.'

environment string delim "\"" "\"" begin
  specialchar = $SPECIALCHAR
end

include "number.lang"

preproc = ':[[:alnum:]\-]+'

variable = '\*[[:alnum:]\-]+\*'

(cbracket,keyword) = `(\()(def[[:alnum:]\-]+)`

(cbracket,function) = `(\()([[:alnum:]\-]+)`

cbracket = "(|)"

symbol = "*|+|-|/|1+|1-|<|=|>"
